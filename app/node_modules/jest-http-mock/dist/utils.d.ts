/// <reference types="node" />
import * as http from 'http';
export declare class Resolvable<T = any> {
    readonly promise: Promise<T>;
    settled: boolean;
    private _resolve?;
    private _reject?;
    constructor();
    resolve: (data: T) => void;
    reject: (error?: any) => void;
}
export declare function bodyReader(request: http.IncomingMessage): () => Promise<Buffer>;
